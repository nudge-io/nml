// Pricing schema fixture in current syntax (the angle-bracket constraint
// syntax this file once used is not part of the language — RFC 0011 cleanup).

model subscription:
    name string
    monthlyPrice money
    annualPrice money
    trialDays number = 14
    maxSeats number?

model multiRegionPricing:
    region string
    price money
    taxRate number
