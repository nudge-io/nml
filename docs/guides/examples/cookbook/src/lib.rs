//! Compiled examples for the cookbook (`docs/guides/`): one example target
//! per recipe page, each run by `just docs-test` and required to print
//! `recipe OK`. The pages' Rust listings are `source=`-synced from
//! `examples/*.rs` — this crate is where the cookbook's code is *true*.
